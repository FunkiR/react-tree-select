import {FontAwesomeIconProps} from '@fortawesome/react-fontawesome';
import {ComponentType} from "react";

export type Props = FontAwesomeIconProps;

export type IconComponent = ComponentType<Props>;
